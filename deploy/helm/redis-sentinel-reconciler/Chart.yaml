apiVersion: v2
name: redis-sentinel-reconciler
description: Sidecar next to Redis/Valkey Sentinel; checks who actually accepts writes
type: application
# Placeholder. A v* tag stamps both via `helm package --version/--app-version`.
version: 0.0.0
appVersion: "0.0.0"
annotations:
  licenses: Apache-2.0

